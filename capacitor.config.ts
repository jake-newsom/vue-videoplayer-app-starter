import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vue-videoplayer-app-starter',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
